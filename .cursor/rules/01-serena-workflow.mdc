---
alwaysApply: false
---
# Serena 工作流程指南

## 🎯 执行条件（自动判断）

当用户消息符合以下任一条件时，本规则自动激活：

### 技术代码指标
- 提到具体代码元素：类名、函数名、文件路径、import、变量
- 提到代码操作：实现、修改、添加、删除、修复、优化、重构、调试
- 提到代码结构：ViewController、View、Model、Request、delegate、protocol
- 提到技术问题：bug、错误、崩溃、异常、不工作、显示问题、布局问题
- 提到技术概念：网络请求、UI布局、JSON解析、异步、多线程、缓存
- 提到项目特定前缀或命名：SS开头的类名、SSBaseRequest、SnapKit、PromiseKit
- 使用 @ 明确触发：@serena、@项目、@代码、@memory

### 判断结果
- **符合任一条件** → 立即执行"强制步骤"
- **都不符合** → 跳过 Serena 工作流，使用通用编码规范

---

## ⚡ 强制步骤（项目任务必须执行）

### 步骤 1：初始化 Serena
```
1. 检测可用的 Serena MCP 服务器（通过工具名称前缀 mcp_serena-* 识别）
2. 调用 initial_instructions
3. 调用 check_onboarding_performed
4. 调用 list_memories
5. 调用 read_memory("project_overview")
6. 在回复开头输出："✅ 已加载项目记忆：project_overview.md"
```

**工具命名规则**: Serena MCP 工具遵循 `mcp_serena-{项目名}_工具名` 格式
- 示例：`mcp_serena-next1_find_symbol`, `mcp_serena-myapp_find_symbol`
- AI 会根据当前工作区自动检测并使用正确的服务器名称

### 步骤 2：按需加载额外记忆
- 涉及第三方库/技术栈 → 读取 `tech_stack`
- 涉及代码实现模式 → 读取 `common_patterns`

### 步骤 3：使用 Serena MCP 工具探索代码

**🚨 严格禁止直接 read_file 读取完整代码文件！**

必须按以下顺序使用 Serena MCP 工具：

#### 探索阶段（了解代码结构）
1. **优先使用** `get_symbols_overview` 获取文件符号概览
2. **然后使用** `find_symbol` 定位具体符号（设置 `include_body=false, depth=1` 查看子符号）
3. **最后使用** `find_symbol` 读取必要的符号体（设置 `include_body=true`）

#### 编辑阶段（修改代码）
- **符号级编辑**：使用 `replace_symbol_body` 替换整个方法/类
- **插入代码**：使用 `insert_after_symbol` 或 `insert_before_symbol`
- **重命名符号**：使用 `rename_symbol`
- **查找引用**：使用 `find_referencing_symbols` 查找符号引用

#### 仅在以下情况使用标准工具
- 非代码文件（JSON、XML、Plist）→ `read_file`
- 已读取过的文件需要小范围修改 → `search_replace`
- 简单文本搜索 → `grep`

---

## ✅ 自我验证

回复前必须确认：
- [ ] 是否调用了 Serena 的 `initial_instructions`？
- [ ] 是否调用了 Serena 的 `check_onboarding_performed`？
- [ ] 是否调用了 Serena 的 `list_memories`？
- [ ] 是否读取了 `project_overview`？
- [ ] 是否告知了用户？
- [ ] **是否优先使用了 Serena MCP 符号化工具而不是 read_file？**
- [ ] 代码是否符合项目规范？

**如果任一项未完成，停止并重新执行。**

---

## 📚 标准文件清单

**必须**:
- `project_overview.md` - 项目概览

**推荐**（按需）:
- `tech_stack.md` - 第三方库详情
- `common_patterns.md` - 代码模式

---

## 🔧 Serena MCP 工具使用决策树

### 何时使用各种工具

| 场景 | 使用工具 | 禁止使用 |
|------|---------|---------|
| 了解文件有哪些类/方法 | `get_symbols_overview` | ❌ `read_file` |
| 查看类的方法列表 | `find_symbol(depth=1, include_body=false)` | ❌ `read_file` |
| 读取单个方法实现 | `find_symbol(include_body=true)` | ❌ `read_file` |
| 替换整个方法 | `replace_symbol_body` | ❌ `search_replace` |
| 在类中添加新方法 | `insert_after_symbol` | ❌ `search_replace` |
| 重命名类/方法 | `rename_symbol` | ❌ 手动查找替换 |
| 查找方法被哪里调用 | `find_referencing_symbols` | ❌ `grep` |
| 搜索字符串模式 | `search_for_pattern` | `grep` 也可 |

### 工作流示例

```
任务：修改 BankAccountViewController 的 loadData 方法

❌ 错误做法：
1. read_file 读取整个文件
2. search_replace 修改方法

✅ 正确做法：
1. get_symbols_overview("BankAccountViewController.swift")
2. find_symbol("BankAccountViewController/loadData", include_body=true)
3. replace_symbol_body("BankAccountViewController/loadData", new_body)
```

---

**详细维护指南**: 使用 `@serena-maintenance` 规则
