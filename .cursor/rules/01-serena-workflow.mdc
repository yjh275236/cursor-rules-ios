---
alwaysApply: true
---

# Serena 工作流程指南

## 🎯 执行条件

**启动 Serena**：
- ✅ 操作项目代码（修改/添加/删除/重构）
- ✅ 分析项目文件（查看/分析具体类名）
- ✅ 技术问题排查（报错/崩溃/不工作）

**跳过 Serena**：
- ❌ 纯概念问题、工具咨询、通用技术讨论

---

## ⚡ 强制步骤（项目任务必须执行）

### 步骤 1：初始化 Serena
```
1. 调用 initial_instructions
2. 调用 check_onboarding_performed  
3. 调用 read_memory("project_overview")
4. 输出："✅ 已加载项目记忆：project_overview.md"
```

### 步骤 2：按需加载额外记忆
- 涉及第三方库/技术栈 → 读取 `tech_stack`
- 涉及代码实现模式 → 读取 `common_patterns`

### 步骤 3：使用 Serena MCP 工具

**🚨 禁止直接 read_file 读取代码文件**

**探索代码**：
1. `get_symbols_overview` 获取文件概览
2. `find_symbol` 定位符号（`include_body=false`）
3. `find_symbol` 读取符号体（`include_body=true`）

**修改代码**：
- 日常修改 → `search_replace`（首选，有 diff 预览）
- 大规模重构 → `replace_symbol_body`、`insert_after_symbol`
- 跨文件重命名 → `rename_symbol`

---

## 📚 标准记忆文件

**必须**:
- `project_overview.md` - 项目概览

**推荐**（按需）:
- `tech_stack.md` - 第三方库详情
- `common_patterns.md` - 代码模式

---

## 🔧 工具选择

- **探索代码**：`get_symbols_overview` → `find_symbol`
- **日常修改**：`search_replace` ✅
- **重构**：`replace_symbol_body` / `insert_after_symbol`
- **重命名**：`rename_symbol`
